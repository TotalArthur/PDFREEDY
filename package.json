{
  "name": "pdfreedy",
  "version": "0.2.0",
  "private": true,
  "description": "P&ID tag finder — local, single-file PDF tag search with text-layer and OCR",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node tests/matching.test.js && node tests/preprocess.test.js && node tests/lineart.test.js && node build.mjs --check && node tests/e2e.test.mjs && node tests/e2e-markup.test.mjs && node tests/e2e-dedup-highlight.test.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.8",
    "esbuild": "^0.28.2",
    "playwright": "^1.56.1",
    "tesseract.js": "^5.0.4"
  }
}
