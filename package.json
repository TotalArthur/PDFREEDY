{
  "name": "pdfreedy",
  "version": "0.2.0",
  "private": true,
  "description": "P&ID tag finder — local, single-file PDF tag search with text-layer and OCR",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node tests/matching.test.js && node tests/preprocess.test.js && node build.mjs --check && node tests/e2e.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "^1.56.1"
  }
}
